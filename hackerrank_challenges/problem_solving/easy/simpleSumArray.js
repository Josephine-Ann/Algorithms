function simpleArraySum(ar) {
    // Write your code here
    var counter = 0;
    for (let i = 0; i < ar.length; i++) {
        counter += ar[i]
    }
    return counter
}

// Given an array of integers, find the sum of its elements.

// For example, if the array 1 2 3 4 10 11 = 31, return 31